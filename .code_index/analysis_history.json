[{"repoId":"Saravanan1689/chroma","confidence":"MEDIUM","errorMessage":"CVE-2023-4641: Password/secret buffer not zeroed on failed authentication attempt","analysedAt":"2026-05-16T15:20:05.243073Z","feedbackReceived":false,"affectedFiles":["chromadb/auth/basic_authn/__init__.py:113"],"suggestedFix":"In the authentication validation function in `chromadb/auth/basic_authn/__init__.py`, after a failed credential comparison, explicitly zero/clear the credential buffer before returning. For Python, use `SecretStr` from pydantic (which avoids storing plaintext) or overwrite the `bytearray` holding the credential:\n\n```python\n# In the validate/authenticate method around line 113:\n# Before:\n#   if not secrets.compare_digest(provided_password, stored_password):\n#       return None  # password left in memory\n#\n# After:\nimport ctypes\n\ndef _zero_string(s: str) -> None:\n    \"\"\"Best-effort zeroing of a Python string's internal buffer.\"\"\"\n    if isinstance(s, str):\n        buf = ctypes.create_string_buffer(len(s))\n        ctypes.memmove(id(s) + sys.getsizeof(s) - len(s) - 1, buf, len(s))\n\n# In the validation block:\ntry:\n    if not secrets.compare_digest(provided_password, stored_password):\n        return None\nfinally:\n    # Zero the provided credential regardless of match outcome\n    if isinstance(provided_password, bytearray):\n        for i in range(len(provided_password)):\n            provided_password[i] = 0\n    # Or better: use SecretStr/bytearray from the start instead of str\n```\n\nAlternatively, refactor to accept credentials as `bytearray` (mutable, can be zeroed) rather than immutable `str`, and wrap with `try/finally` to ensure zeroing on all code paths including exceptions.","branch":"main","errorSignature":"15d0ab609b3f2cccaaed55ee9fe9b5e5e783311f1bf8c3a36ded927400b0b4fc","rootCause":"Based on the similar past bug reference (CVE-2023-4641 fixed in `chromadb/auth/basic_authn/__init__.py:113`), the vulnerability is in the basic authentication provider's credential validation flow. When a password/token comparison fails during authentication, the buffer holding the submitted credential (likely a Python `str` or `bytes` object) is not explicitly zeroed or cleared before the function returns. This leaves the plaintext secret readable in process memory. The fix was previously applied at line 113 of that file, suggesting the `authenticate()` or credential validation method stores the user-supplied password in a local variable that persists after a failed comparison without being overwritten.","chunksSearched":19582,"id":"16ff667a-7a9d-49a6-af7d-69070a2df09e","stackTrace":"Security audit finding:\nCWE-312: Cleartext Storage of Sensitive Information\nCWE-316: Cleartext Storage of Sensitive Information in Memory\nCVE-2023-4641 pattern: shadow-utils passwd fails to clear password buffer on second entry mismatch\nAffected pattern:\n  1. User enters password (stored in buffer/String)\n  2. User asked to confirm password\n  3. Confirmation fails or mismatches\n  4. First password entry NOT zeroed from memory\n  5. Memory remains readable by attacker with memory access\nLook for: password validation, token comparison, secret confirmation, credential handling, authentication middleware","chunksUsed":119},{"repoId":"Saravanan1689/chroma","confidence":"HIGH","errorMessage":"CVE-2023-4641: Password buffer not zeroed on failed authentication","analysedAt":"2026-05-16T15:18:46.706886Z","feedbackReceived":false,"affectedFiles":["chromadb/auth/basic_authn/__init__.py:113","chromadb/auth/basic_authn/__init__.py:117"],"suggestedFix":"In `authenticate_or_raise`, add a `finally` block to explicitly delete the sensitive local variables after use. While Python's immutable strings cannot be truly zeroed, this minimizes their lifetime and reference count:\n\n```python\ndef authenticate_or_raise(self, headers: Dict[str, str]) -> UserIdentity:\n    base64_decoded = None\n    password = None\n    try:\n        if AUTHORIZATION_HEADER.lower() not in headers.keys():\n            raise AuthError(AUTHORIZATION_HEADER + \" header not found\")\n        _auth_header = headers[AUTHORIZATION_HEADER.lower()]\n        _auth_header = re.sub(r\"^Basic \", \"\", _auth_header)\n        _auth_header = _auth_header.strip()\n\n        base64_decoded = base64.b64decode(_auth_header).decode(\"utf-8\")\n        if \":\" not in base64_decoded:\n            raise AuthError(\"Invalid Authorization header format\")\n        username, password = base64_decoded.split(\":\", 1)\n        username = str(username)\n        password = str(password)\n        if username not in self._creds:\n            raise AuthError(\"Invalid username or password\")\n\n        _pwd_check = bcrypt.checkpw(\n            password.encode(\"utf-8\"),\n            self._creds[username].get_secret_value().encode(\"utf-8\"),\n        )\n        if not _pwd_check:\n            raise AuthError(\"Invalid username or password\")\n        return UserIdentity(user_id=username)\n    except AuthError as e:\n        logger.error(\n            f\"BasicAuthenticationServerProvider.authenticate failed: {repr(e)}\"\n        )\n    except Exception as e:\n        tb = traceback.extract_tb(e.__traceback__)\n        last_call_stack = tb[-1]\n        line_number = last_call_stack.lineno\n        filename = last_call_stack.filename\n        logger.error(\n            \"BasicAuthenticationServerProvider.authenticate failed: \"\n            f\"Failed to authenticate {type(e).__name__} at {filename}:{line_number}\"\n        )\n    finally:\n        del password\n        del base64_decoded\n    time.sleep(random.uniform(0.001, 0.005))\n    raise ChromaAuthError()\n```\n\nFor a more robust fix, use `bytearray` for the password encoding (which can be zeroed) instead of encoding to an immutable `bytes` object, and zero it after use:\n```python\n    password_bytes = bytearray(password.encode(\"utf-8\"))\n    try:\n        _pwd_check = bcrypt.checkpw(\n            bytes(password_bytes),\n            self._creds[username].get_secret_value().encode(\"utf-8\"),\n        )\n    finally:\n        for i in range(len(password_bytes)):\n            password_bytes[i] = 0\n```","branch":"main","errorSignature":"6c2d5821d3e83ea134cc76a216836bc66783de32bf531cb08c3ec37f2a14a97f","rootCause":"In `chromadb/auth/basic_authn/__init__.py`, the `authenticate_or_raise` method (lines 105-147) stores the decoded password in a local variable `password` (line 117) but never explicitly clears/zeroes this variable after a failed authentication check. When `bcrypt.checkpw` returns `False` (line 122) or when the username is not found (line 119), the function raises `AuthError` which is caught, but the `password` string remains in memory. Additionally, `base64_decoded` (line 113) containing the full `username:password` cleartext also persists in memory. Python strings are immutable so they cannot be securely wiped, but the code makes no attempt to minimize the lifetime of these sensitive variables (e.g., by deleting references with `del password` and `del base64_decoded` in a `finally` block).","chunksSearched":19582,"id":"423cede7-d509-4043-8c6e-302d971bf84a","stackTrace":"Security audit finding:\nCWE-316: Cleartext Storage of Sensitive Information in Memory\nLook for: password validation, token comparison, credential handling, authentication middleware","chunksUsed":39}]